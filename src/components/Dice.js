/*
import React, { Component } from 'react';
import {} from 'three';
import {} from 'cannon';
import DiceBox from './DiceBox';

class Dice extends Component {
  constructor (container){
    console.log(container);
    const diceBox = new DiceBox(container, null);
    console.log('creating Dice')
    this.test = "test"
  }
}

export default Dice;

*/
